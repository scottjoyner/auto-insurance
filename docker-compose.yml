services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-auto_insurance}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

  quote-service:
    build:
      context: .
      dockerfile: services/quote-service/Dockerfile
    environment:
      QUOTE_SERVICE_DEFAULT_PRODUCT_YAML: /app/data/sample-products/sample_personal_auto_v1.yml
      QUOTE_SERVICE_ALLOWED_ORIGINS: ${QUOTE_SERVICE_ALLOWED_ORIGINS:-http://localhost:3000,http://localhost:8000}
      QUOTE_SERVICE_ALLOW_CREDENTIALS: ${QUOTE_SERVICE_ALLOW_CREDENTIALS:-false}
      QUOTE_SERVICE_DATABASE_URL: ${QUOTE_SERVICE_DATABASE_URL:-sqlite:////app/quote_service.db}
      QUOTE_SERVICE_AUTO_CREATE_SCHEMA: ${QUOTE_SERVICE_AUTO_CREATE_SCHEMA:-true}
    ports:
      - "8001:8001"
    depends_on:
      postgres:
        condition: service_healthy

  risk-appetite-service:
    build:
      context: .
      dockerfile: services/risk-appetite-service/Dockerfile
    environment:
      RISK_APPETITE_POLICY_PATH: /app/services/risk-appetite-service/data/risk-appetite-policy.yml
      RISK_APPETITE_ALLOWED_ORIGINS: ${RISK_APPETITE_ALLOWED_ORIGINS:-http://localhost:3000,http://localhost:8000}
      RISK_APPETITE_ALLOW_CREDENTIALS: ${RISK_APPETITE_ALLOW_CREDENTIALS:-false}
      RISK_APPETITE_ENABLE_RUNTIME_POLICY_UPDATE: ${RISK_APPETITE_ENABLE_RUNTIME_POLICY_UPDATE:-false}
      RISK_APPETITE_DATABASE_URL: ${RISK_APPETITE_DATABASE_URL:-sqlite:////app/risk_appetite_service.db}
      RISK_APPETITE_AUTO_CREATE_SCHEMA: ${RISK_APPETITE_AUTO_CREATE_SCHEMA:-true}
    ports:
      - "8002:8002"
    depends_on:
      postgres:
        condition: service_healthy

  policy-service:
    build:
      context: .
      dockerfile: services/policy-service/Dockerfile
    environment:
      POLICY_SERVICE_ALLOWED_ORIGINS: ${POLICY_SERVICE_ALLOWED_ORIGINS:-http://localhost:3000,http://localhost:8000}
      POLICY_SERVICE_ALLOW_CREDENTIALS: ${POLICY_SERVICE_ALLOW_CREDENTIALS:-false}
      POLICY_SERVICE_DATABASE_URL: ${POLICY_SERVICE_DATABASE_URL:-sqlite:////app/policy_service.db}
      POLICY_SERVICE_AUTO_CREATE_SCHEMA: ${POLICY_SERVICE_AUTO_CREATE_SCHEMA:-true}
    ports:
      - "8003:8003"
    depends_on:
      postgres:
        condition: service_healthy

  claims-service:
    build:
      context: .
      dockerfile: services/claims-service/Dockerfile
    environment:
      CLAIMS_SERVICE_ALLOWED_ORIGINS: ${CLAIMS_SERVICE_ALLOWED_ORIGINS:-http://localhost:3000,http://localhost:8000}
      CLAIMS_SERVICE_ALLOW_CREDENTIALS: ${CLAIMS_SERVICE_ALLOW_CREDENTIALS:-false}
      CLAIMS_SERVICE_DATABASE_URL: ${CLAIMS_SERVICE_DATABASE_URL:-sqlite:////app/claims_service.db}
      CLAIMS_SERVICE_AUTO_CREATE_SCHEMA: ${CLAIMS_SERVICE_AUTO_CREATE_SCHEMA:-true}
    ports:
      - "8004:8004"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres-data:

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "insurance-events"
version = "0.1.0"
description = "Event publisher abstractions for the insurance operating system"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["insurance_events"]

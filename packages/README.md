# Packages

This directory will contain shared packages used by services and contracts.

Planned packages:

- shared-types
- event-schemas
- product-config
- rating-dsl
- authz
- observability

Shared packages should be versioned carefully because they define cross-service contracts.

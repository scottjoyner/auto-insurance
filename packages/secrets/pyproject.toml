[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "insurance-secrets"
version = "0.1.0"
description = "Secret provider abstractions for production deployments"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["insurance_secrets"]

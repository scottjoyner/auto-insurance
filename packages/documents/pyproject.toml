[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "insurance-documents"
version = "0.1.0"
description = "Document generation scaffolds for policies and adverse-action notices"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["insurance_documents"]
